name: Github CI with Local Runner

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  run_tests:
    runs-on: self-hosted
    steps:
      - name: 📥 Чекаут кода
        uses: actions/checkout@v4

      - name: 🔧 Подготовка окружения
        run: |
          python -m venv venv
          .\venv\Scripts\Activate.ps1
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🧪 Запуск тестов
        run: |
          .\venv\Scripts\Activate.ps1
          pytest tests/ --alluredir=./allure-results --clean-alluredir -v
        continue-on-error: true
        env:
          TOKEN: ${{ secrets.TOKEN }}


      - name: 📊 Генерация Allure HTML
        if: always()
        run: allure generate ./allure-results -o ./allure-report --clean


      # Выкладываем allure-отчёт на Github Pages
      - name: Deploy report to Github Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-report


#      - name: 📤 Загрузка отчета в артефакты
#        if: always()
#        uses: actions/upload-artifact@v5
#        with:
#          name: allure-report
#          path: ./allure-report
#
#      - name: 🚀 Публикация в Github Pages (ручной пуш)
#        if: always()
#        env:
#          TOKEN: ${{ secrets.SECRET_TOKEN }}
#        run: |
#          Remove-Item -Recurse -Force /tmp/allure-report -ErrorAction SilentlyContinue
#          Copy-Item -Recurse ./allure-report /tmp/allure-report
#          git config --global user.name "Github CI"
#          git config --global user.email "ci@Github.ru"
#          try { git fetch origin gh-pages } catch { Write-Host "No remote gh-pages" }
#          git checkout -B gh-pages
#          try { git merge origin/gh-pages --allow-unrelated-histories -m "Merge" } catch { Write-Host "No merge" }
#          Get-ChildItem -Exclude .git | Remove-Item -Recurse -Force
#          Copy-Item -Recurse /tmp/allure-report/* ./
#          git add .
#          try { git commit -m "Deploy Allure Report" } catch { Write-Host "Nothing to commit" }
#          git push https://$env:TOKEN@github.com/Kirill-Kamyshanov/memes gh-pages --force
#
#      - name: 🔗 Ссылка на Allure Отчет
#        if: success()
#        run: |
#          REPORT_URL="https://${{ github.repository_owner }}.github.site/${{ github.event.repository.name }}/"
#          echo "Отчет успешно опубликован!"
#          echo "Открыть Allure: $REPORT_URL"